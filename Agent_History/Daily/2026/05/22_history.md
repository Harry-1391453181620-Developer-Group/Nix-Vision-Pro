# 2026-05-22 History

## Request

Renamed the project to `Nix Vision Pro` across the active repository.

## Work completed

- Renamed the training guide file to `Nix_Vision_Pro.md`.
- Updated README branding to `Nix Vision Pro`.
- Updated the NumPy and PyTorch GUI window titles to use `Nix Vision Pro`.
- Updated the NumPy and PyTorch `--help-md` guide path references to the renamed guide file.
- Updated tracked history and plan documents that still mentioned the old project name or the old training guide filename.

## Validation

- Searched the repository for stale pre-rename project-name tokens before and after the rename pass.
- Verified the renamed training guide is now the file targeted by both backend training help flows.

## Notes

- The current workspace directory on disk still uses the older folder name; this rollout only updated in-repo references and tracked content.
